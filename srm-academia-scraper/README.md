# SRM Academia Scraper (Phase 1: Authentication)

This project is being built from `REQUIREMENTS.md`.

## Requirements

```bash
pip install -r requirements.txt
```

## Run

```bash
python main.py
```

Set environment variables to avoid prompts:

- `SRM_EMAIL`
- `SRM_PASSWORD`

Optional debug mode:

- `SRM_DEBUG=1`
